# IDEAS
- [x] Integrate with ElevenLabs or Kokoro WebGPU for high-quality TTS instead of relying on the native, robotic browser SpeechSynthesis API.
- [x] Implement IndexedDB caching for large `.vrm` assets to prevent downloading the 10MB model on every page refresh.
- Expand local storage persistence to save animation states and UI configurations across sessions.
- [x] Introduce a 3D environment loading system (e.g., bedrooms, sci-fi decks) via `.glb` files to replace the static dark background.
